/* FOR LOOPS
Works in the same way as JS:
for (let i = 0; i < 5; i++) {
  // code to run
}

Args:
- Initalization: let i = 0
- Continue condition. False -> loop stops: i < 5
- value change: i++

I skipped the coding exercises for this part as loops work in the same way as in JS.
*/
