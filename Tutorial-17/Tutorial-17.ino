/* WHILE LOOPS
Works in the same way as JS:
for (condition) {
  // code to run
}

Bit pointless for arduino ino files as they run in a loop function already.
But good for things like waiting for a button press or sensor input change before the code continues to run.

Skipping the tutorial exercises as while loops work in the same way as in JS.
*/
